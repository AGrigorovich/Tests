import React, {Component} from 'react';

class BaseComponent extends Component {
    render() {
        return (
                <fieldset>
                    <legend>I AM</legend>
                </fieldset>
        )
    }
}

export default BaseComponent;
